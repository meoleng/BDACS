name: Crypto_Notice_Bot

on:
  schedule:
    # 매 10분마다 실행
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
   steps:
      - name: 코드 가져오기
        uses: actions/checkout@v4  # 🌟 v4로 변경

      - name: 파이썬 설정
        uses: actions/setup-python@v5 # 🌟 v5로 변경
        with:
          python-version: '3.9'

      - name: 라이브러리 설치
        run: pip install requests beautifulsoup4

      - name: 봇 실행
        env:
          SLACK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: python notice.py

      - name: 알람 기록 파일 저장 (중복 방지용)
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add notified_list.txt
          git commit -m "Update notified_list.txt" || echo "No changes to commit"
          git push
